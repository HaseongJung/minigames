import Timer from "../assets/timer.png";

const ResponseCheckLogo = () => {
    return(
        <img src={Timer} alt="Timer logo" style={{width: 40, height: 40}}/>
    );
}
export default ResponseCheckLogo;